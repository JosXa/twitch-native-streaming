# twitch-native-streaming
See which twitch channels are online and watch on your native client

# Requirements
livestreamer
`$ sudo pacman -S livestreamer`

# Usage
Run `$ python3 twitch --help` to see what you can do
